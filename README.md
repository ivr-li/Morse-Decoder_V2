# 🎧 MorseNet: Декодер аудио файлов с кодом морзе с помощью нейросетей.

## 🔍 О проекте
MorseNet — это нейросетевая модель для автоматического декодирования аудиофайлов с морзе в текст. Проект включает:
- Модель MorseNet на PyTorch
- FastAPI-сервер для инференса
- Набор инструментов для переобучения/дообучения и тестирования.

## 📊 Метрика качества: Levenshtein mean — усредненное расстояние Левенштейна между истинными сообщениями и сообщениями, предсказанными моделью. 
`Чем меньше расстояние, тем качественнее перевод`
Инференс на тренировочных данных показал результат по методу Левенштейна:
`На тестовых данных составляет: 95%`
`На валидационных данных составляет: 97%`

Загрузка тестовых данных на Kaggle показала качество: **0.433** по усредненному расстоянию Левенштейна, 15 место в лидерборде. Лучшая модель в лидерборде показала качество **0.24**.
![](Kagle.png)
## ⚙️ Установка локального сервера (без Docker).
1. **Клонирование репощитория:**
    ```
    git clone https://github.com/SalLangg/Morse-Decoder_V2
    cd Morse-Decoder_V2
    ```
2. **Создание и активация:**
   - Windows:
    ```
    python -m venv venv
    .venv\Scripts\activate
    ```
   - Linux:
    ```
    source venv/bin/activate
    ```
3. **Установка зависимостей:**
    ```
    pip install -r requirements.txt
    ```
4. **Запуск сервера FastAPI:**
   ```
   python -m uvicorn main:app --reload
   ```
## 🐳 Запуск через Docker
1. **Соборка образа:**
   ```
   docker build -t morsenet-app .
   ```
2. **Запуск контейнера:**  
   ```
   docker run --rm -it -p 8000:8000 morsenet-app
   ```

После запуска, приложение будет доступно по адресу:  
🔗 API: http://127.0.0.1:8000  
📘 Документация: http://127.0.0.1:8000/docs  
💮 MLflow-сервер: http://127.0.0.1:5001

## 📂 Структура проекта.
```
morse_decoder/
├── 📂notebooks/  #Эксперементы
│   ├── ...
│   └── чек-лист  #чек-лист эксперементов по построению ахитектуры и попыткам улучщения качества модели. 
|              
├── 📂src_data/             
|   ├── 📂loaded_audio/   # Данные, что будут использоваться для тренировки моделей на сервере.
|   ├── 📂saved_models/
|   |   └── MorseNet.pth       # Базовая модель MorseNet
|   └── 📂*morse_dataset/     # Базовый датасет, на котором обучалась модель MorseNet (создается пользователем)
|       ├── morse_dataset
|       ├── test.csv  
|       └── train.csv
|
├── 📂src_decoder/
|   ├── 📂configs/            
|   │   ├──⚙️config.yaml     # Параметры для тестирования моделей
|   │   └──⚙️config.py       # Базовые значения параметров
│   ├── 📂data/                 
│   │   └── dataset.py        # Класс Dataset
│   └── 📂models/
│       ├── BaseModel.py      # Базовый класс моделей на сервере
│       └── MorseNet.py       # Архитектура основной модели MorseNet
|
├── README.md
├── requirements.txt        
├── train.py                   # FastAPI router для обучения и вывода новых моделей
├── inference.py               # FastAPI router для инференса MorseNet
└── 🚀main.py                  # Точка входа в FastAPI
```
## 🧠 MorseNet: Как это работает.
### 🏠 Архитектура модели.
MorseNet использует гибридную архитектуру:
1. **CNN-блок** (4 слоя) для извлекает признаки из спектрограмм  
   `[batch, time(sequence), channels, mels]`
2. **Linear1 + GELU слои** преобразует пространство признаков CNN в пространство пригодное для LSTM  
   `[batch, time, channels, mels]` → `[batch, time, channels*mels]` → `[batch, time, N_MELS*2]`
3. **LSTM-блок** (2 слоя) для анализа временных зависимостей
   → `[batch, time, N_MELS*2]`
4. **LayerNorm + Dropout(0.5) слои** используются для борьбы с переобучением. _Для RNN сетей Dropout больше чем обычно!_
5. **Linear2** переводит размерность вектора к размерности пригодной для классификации  
   `[batch, time, N_MELS*2]` → `[batch, time, num_classes]`
6. **log_softmax** формирует из полученных значений логиты для функции потерь
   `[batch, time, num_classes]` → `[time/T,batch/N,num_classes/C]`

### 📈 Настройка обучения модели.
| Компонент               | Описание                       | Ресурсы |
|-------------------------|--------------------------------|--------|
| **Adam**                |Оптимизатор    | [Adam](https://docs.pytorch.org/docs/stable/generated/torch.optim.Adam.html#torch.optim.Adam) |
| **ReduceLROnPlateau**   | Метод уменьшения шага обучения | [ReduceLROnPlateau](https://docs.pytorch.org/docs/stable/generated/torch.optim.lr_scheduler.ReduceLROnPlateau.html#torch.optim.lr_scheduler.ReduceLROnPlateau) |
| **CTC**  | Функция потерь| [CTC architectures](https://huggingface.co/learn/audio-course/en/chapter3/ctc)   · [CTC loss](https://github.com/shouxieai/CTC_loss_pytorch?tab=readme-ov-file) |
   ```
   # Функция потерь
   self._loss_func = nn.CTCLoss(
      blank=self.blank,         # Индекс blank-символа (для CTC)
      reduction='mean',         # Усреднение потерь по батчу
      zero_infinity=True        # Стабилизация вычислений для длинных последовательностей
   ).to(self.device)             # Перенос на GPU (если доступно)
   
   loss = self._loss_func(
       log_probs=predict,              # Тензор [T, N, C] (time(sequence), batch, num_classes)
       targets=targets,                # Выровненный по длине вектор целевых последовательностей [N, S]
       input_lengths=predict_lengths,  # Длины прогнозов [N]
       target_lengths=targets_lens     # Длины целей [N]
   )
   ```
### 🔸Ключевые особенности.
- Обработка аудио через Mel-спектрограммы
- Аугментация данных (Time/Frequency Masking)
- Поддержка русского алфавита и цифр
### 🔨 Подходы и эксперименты.
**Базовая модель** — гибрид стандартных архитектур CNN и RNN (LSTM) с CTC loss
На базовом решении достигнуто **качество 0.43 по Левенштейну** (лучшие модели на Kaggle дают ~0.25).
Целью дальнейших экспериментов было улучшение качества без чрезмерного усложнения архитектуры и увеличения времени обучения

---

#### 🧪 Этапы экспериментов (Полная версия в notebook/Чек лист).

##### ⚙️ 1. Дебаг обучения:
- Отладка **взрывов градиента** и `NaN` значений — проблема оказалась в выравнивании и паддинге
- Переписан **декодер**: устранен баг в логике CTC-декодирования, улучшивший итоговую метрику
- Исправлена путаница между **padding-символом и blank-символом CTC** — введен отдельный индекс

##### 🔄 2. Архитектура и гиперпараметры:
- Перебор: `learning_rate`, `weight_decay`, `dropout`, `GRU hidden`, `num_layers`, `batch_size`
- Изменение архитектуры CNN: глубина, `kernel_size`, `stride`, `padding`, удаление `MaxPool`
- Добавление **Dropout(0.3–0.5)** в рекуррентные блоки — снижает переобучение
- Использование `clip_grad_norm`, `flatten_parameters`, `DataParallel`

##### 🧩 3. Улучшения CNN:
- Добавление [**self-attention в CNN-блоках**](https://arxiv.org/pdf/2506.10683v1.pdf) — улучшение незначительное, чувствительно к переобучению
- Снижение агрессивности `MaxPool` — в теории должны сохранить больше признаков по временной оси, но сильного клияния на кчество замечено небыло

##### 🔁 4. Улучшения RNN:
- Увеличение `num_layers=3` - усилило переобучение
- Уменьшение `GRU hidden` отсрточило переобучение
- Попытки добавить **attention / multi-head attention** — не дали прироста (отсутствуют зависимости между токенами)
- Замена GRU на LSTM - более стабильный процесс обучения

##### 📊 5. Аугментации и данные:
Испытаны:
- Различные значения `TimeMasking`, `FrequencyMasking` - оптимальные значения получились 15 и 20
- Аугментации через `audiomentations` - сильно усложнили процесс обработки данных и не дали прирост качества
- Предобработка спектрограмм — ускоряет обучение, но не влияет на метрику

##### 🔧 6. Ансамблирование моделей:
- Попытка ансамблировать модели привела к падению качества с 0.42 до 0.44
- Attention и SE-блоки не превзошли по качеству простую версию CNN+LSTM

---

#### ✅ Выводы
- **Наилучшее качество (0.43)** достигнуто простой моделью CNN → LSTM → CTC без attention, но с точной настройкой гиперпараметров и корректным декодером
- **Attention** не дает улучшений: в тренировочном датасете нет зависимостей между символами, в отличие от естественной речи
- **SE-блоки** полезны, но не критичны

### 📌Применение технологии  
На основе архитектуры MorseNet можно разрабатывать:  
- Автоматические переводчики аудио
- Системы анализа радиопередач
- Компоненты для обработки аудио в реальном времени.

# 🚀 FastAPI Сервер MorseNet
### 🌐 Обзор API
```
📂 FastAPI Server (MorseNet)
├── 🔄 inference_lifespan     # Инициализация базовой модели для инференса
├── 🔄 train_lifespan         # Инициализация локального сервера MLflow
│
├── 📌 /ML_inference
│   ├── POST /load            # Загрузка пользовательского аудиофайла
│   ├── POST /predict         # Декодирование загруженного файла
│   ├── POST /upload_model    # Загрузка новой модели в инференс
│   └── DELETE /delete_all    # Удаление всех загруженных пользовательских файлов
│
├── 📌 /ML_training
|   ├── POST /load_{model}    # Загрузка модели
|   ├── PUT /loading_data     # Загрузка датасета для обучения/переобучения модели
|   ├── POST /fit             # Обучение модели
|   └── POST /fit_inference   # Инференс модели на тестовых данных
|
└── /base_data                # Загрузка базового датасета на сервер
```

## 🔍 Ключевые компоненты
1. Инициализация базовых гиперпараметров и базовых классов MorseNet и MorseDataset на старте сервера для ускорения декодирования в реальном времени. (`inference.py/lifespan`)
2. Возможность для дообучения/переобучения загружать свой набор гиперпараметров, изолированно от инференса (`config.py`).
3. Дообучение/переобучение напрямую на сервере, с возможностью загрузки подготовленных данных(.rar) на сервер по ссылке Google Drive.
4. Логирование метрик в реальном времени через локальный сервер MLflow.
   На каждой эпохе (epoch) записываются:
    - Градиенты (в %)
    - Потери (тренировочные и валидационные)
    - Learning rate (текущий)
5. Автоматическое сохранение обученной модели.
6. Возможность загрузить обновленную модель в инференс без перезагрузки сервера. Достаточно заменить файл .pkl и загрузить новую модель.
